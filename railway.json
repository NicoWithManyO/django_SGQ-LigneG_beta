{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate contenttypes && python manage.py migrate auth && python manage.py migrate admin && python manage.py migrate sessions && python manage.py migrate catalog --fake && python manage.py migrate production && python manage.py migrate quality && python manage.py migrate wcm && python manage.py migrate planification && python manage.py migrate livesession && python manage.py migrate frontend && python manage.py collectstatic --noinput && gunicorn sgq_ligne_g.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}